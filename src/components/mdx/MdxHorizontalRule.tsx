import React from 'react';
import { Box } from '@chakra-ui/react';

const MdxHorizontalRule = () => (
  <Box bg="gray.300" h={1} position="absolute" left={0} w="100%" mt="-1rem" zIndex={-1} />
);

export default MdxHorizontalRule;
